智能体与环境的交互逐渐成熟，视觉方向从过往的各自独立的任务逐渐合并向一个大的工作
video model 与3D空间相关的研究的关系
因为当前videomodel发展的比较迅速只需要输入图像即可生成相关内容，是否3D相关研究可以并入videlmodel

CVPR2026 best paper deepmind D4RT

1、2D vision
pix dit
下一代2D视觉生成模型的发展方向是什么，实现形式是什么
多模统一视觉模型是趋势，不仅是视觉和语言还包含3D

端侧部署
模型不能特别大，对于芯片压力较大，但同样的小模型效果打折扣
云+端是推荐的方法

2、图像编辑与生成

## Objects365-2020 数据集下载记录

官方地址：
https://data.baai.ac.cn/datadetail/Objects365_2020

### 下载前判断

Objects365-2020 官方页面对应的数据集总大小约 380.58 GB。当前本机磁盘情况是：

- C 盘剩余空间很少，不适合放数据集。
- D 盘剩余约 441 GB，可以放压缩包，但如果再解压、生成索引、缓存特征，会非常紧张。

因此不建议把数据集直接放在 Obsidian/项目目录 `D:\5AM` 下面。推荐单独放到：

```powershell
D:\datasets\objects365\
  raw\
  annotations\
  images\
  manifests\
```

### 推荐下载顺序

不要一开始全量下载 train/test。当前 DINO-GeoSemMap 更需要先验证 Objects365 对室内检测类别覆盖的收益，因此建议分阶段：

1. 先下载标注文件，例如 `zhiyuan_objv2_train.json.tar.gz` 以及 validation 标注。
2. 再下载 validation 图片包，用来跑通类别统计、映射和 dataloader。
3. 然后选择部分 train 图片分包，先做小规模训练或预训练验证。
4. 最后再决定是否全量下载 51 个 train 图片包。
5. 暂时跳过 test set，因为它对当前训练闭环帮助不大。

### BAAI 登录与 token

BAAI 页面是前端渲染页面，文件列表和下载链接通常需要登录后的 token。不要保存账号密码，只需要浏览器登录后的 access token 或者网页直接生成的下载链接。

获取 token 的一种方式：

1. 打开官方页面并登录。
2. 浏览器按 `F12`。
3. 进入 `Application` -> `Local Storage` -> `https://data.baai.ac.cn`。
4. 找到 `data-appToken`。
5. 复制其中的 `accessToken` 字段。

也可以在 `Network` 面板里找到 `getFileList` 或 `getFileSign` 请求，复制请求头里的：

```text
Authorization: Bearer <accessToken>
```

### 查询文件列表

已知数据集元信息：

- bucketName: `baai-datasets`
- datasetId: `94`
- objPrefixKs3: `fc5f8a7cf75e52911753d726512224d2`

PowerShell 查询模板：

```powershell
$token = "这里填 accessToken"
$headers = @{
  Authorization = "Bearer $token"
  "content-type" = "application/json"
}

$body = @{
  bucketName = "baai-datasets"
  pageSize = 100
  pageNumber = 1
  prefix = "fc5f8a7cf75e52911753d726512224d2/"
} | ConvertTo-Json

Invoke-RestMethod `
  -Uri "https://data.baai.ac.cn/api/datahub/datasetFile/v1/getFileList" `
  -Method Post `
  -Headers $headers `
  -Body $body
```

### 生成下载链接

从文件列表里拿到具体 `filePath` 后，再请求签名下载链接：

```powershell
$signBody = @{
  bucketName = "baai-datasets"
  datasetId = 94
  type = 1
  filePath = "这里填文件列表里的 filePath"
} | ConvertTo-Json

Invoke-RestMethod `
  -Uri "https://data.baai.ac.cn/api/datahub/datasetFile/v2/getFileSign" `
  -Method Post `
  -Headers $headers `
  -Body $signBody
```

### 下载工具

大文件下载建议使用 `aria2c`，支持断点续传和多连接。当前机器还未检测到 `aria2c`，可以先安装：

```powershell
winget install aria2.aria2
```

下载模板：

```powershell
aria2c -c -x 8 -s 8 --auto-file-renaming=false --dir "D:\datasets\objects365\raw" "这里填签名下载链接"
```

参数说明：

- `-c`：断点续传。
- `-x 8 -s 8`：最多 8 连接下载。
- `--auto-file-renaming=false`：避免重复下载时自动改名导致文件混乱。
- `--dir`：保存目录。

### 接入 DINO-GeoSemMap 的后续步骤

下载完成后不要急着全量解压训练，先做三个轻量步骤：

1. 解析 Objects365 标注里的 categories，生成类别表。
2. 建立 `Objects365 category -> DINO-GeoSemMap Top-30/Top-50` 的类别映射。
3. 根据目标类别生成 JSONL manifest，只引用需要的图片和标注。

这样可以避免把 380GB 数据全部搬进训练流程，也能先判断 Objects365 对 `cabinet`、`bookshelf`、`door`、`window`、`wardrobe`、`nightstand`、`coffee_table` 等室内类别是否真的带来增益。


0611 地瓜云平台讨论
数据部分包含人工和自动  数据清洗
具身智能工作站-issaic sim云端部署迁移
抢占式占用式？
量化模型如果板端没有适配如何进行一键下发
云平台中存储可使用量是多少，如果我期望在云平台中进行全量的微调算力是否能支持
快速下发的板子是否可以添加自己的